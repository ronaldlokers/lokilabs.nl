# build output
dist/
# generated types
.astro/
# local wrangler dev state
.wrangler/
# playwright MCP screenshots/logs from local testing
.playwright-mcp/
# playwright test runner output
playwright-report/
test-results/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# product-team-review tracking (not for commit)
.product-team-review/

# impeccable live-mode session journals (local recovery state)
.impeccable/live/sessions/

# impeccable critique runs — point-in-time audits that go stale the moment the
# findings are fixed. One was committed still reporting a P0 contrast failure
# that had already been resolved, on a repo read as portfolio evidence.
.impeccable/critique/
